# 0015 profile group ordering

Adds a closed group sort mode. Existing groups default to manual order; the
second allowed value enables alphabetical presentation. Per-profile saved order
remains unchanged.
