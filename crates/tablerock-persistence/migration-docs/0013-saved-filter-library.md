# 0013 saved filter library

Adds one bounded named-filter library document per profile. The JSON contract is
limited to preset names, object scope, raw-WHERE text, and typed filter clauses;
it has no credential or result-cell storage. Application decoding owns schema
validation beyond the database byte bound.
