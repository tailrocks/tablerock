# 0002 — Support facts

## Before

The database contained only its sequential migration ledger.

## After

`support_facts` provides a keyed, timestamped location for verified local
database/runtime compatibility facts. It contains no profile, credential,
query, result, or cell values.
