# Design artifacts

`TableRock-Native-Concepts.sketch` was operator-rejected and removed from Git
history on 2026-08-12. It is not a requirement, visual reference, component
source, or implementation input. New native macOS design work must not derive
any decision from it.

The active exploration uses Apple's current macOS guidance and clean-room
observation of TablePro's public interface. See the [phase 0 architecture and
drift report](../docs/design/native-macos-2026/phase-0-architecture-and-drift.md).

Do not restore the rejected artifact.
