name: Publish immutable package release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: read

concurrency:
  group: package-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    # Tag pushes are post-merge push events: the Velnor fleet rejects those
    # (merged_push_occupancy), so push routes to the GitHub lane.
    runs-on: ${{ github.event_name == 'push' && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 10
    outputs:
      version: ${{ steps.tag.outputs.version }}
      source-ref: ${{ steps.tag.outputs.source_ref }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - id: tag
        env:
          TAG: ${{ github.ref_name }}
          REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          [[ "$TAG" =~ ^v[0-9]+[.][0-9]+[.][0-9]+$ ]]
          test "$REF" = "refs/tags/$TAG"
          version=${TAG#v}
          manifest_version=$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)
          test "${manifest_version%-dev}" = "$version"
          {
            echo "version=$version"
            echo "source_ref=$REF"
          } >> "$GITHUB_OUTPUT"

  build-cli:
    needs: validate
    # macOS legs stay GitHub-hosted (Velnor is Linux-only); Linux legs route to
    # the GitHub lane on push (merged_push_occupancy) and Velnor otherwise.
    runs-on: ${{ matrix.os == 'github-linux' && 'ubuntu-26.04' || matrix.os == 'velnor-linux' && fromJSON('["self-hosted","velnor-target-mvp"]') || matrix.os }}
    timeout-minutes: 45
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - {target: aarch64-apple-darwin, os: macos-26, builder: cargo}
          - {target: x86_64-apple-darwin, os: macos-26, builder: cargo}
          - {target: aarch64-unknown-linux-gnu, os: "${{ github.event_name == 'push' && 'github-linux' || 'velnor-linux' }}", builder: zigbuild, build_target: aarch64-unknown-linux-gnu.2.17}
          - {target: x86_64-unknown-linux-gnu, os: "${{ github.event_name == 'push' && 'github-linux' || 'velnor-linux' }}", builder: zigbuild, build_target: x86_64-unknown-linux-gnu.2.17}
    env:
      CARGO_INCREMENTAL: "0"
      RUSTC_WRAPPER: sccache
      SCCACHE_GHA_ENABLED: "false"
      SCCACHE_CACHE_SIZE: 20G
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          install_args: rust
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        if: matrix.builder == 'zigbuild'
        with:
          mise_toml: |
            [tools]
            zig = "0.16.0"
            "cargo:cargo-zigbuild" = "0.23.0"
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba # v0.0.11
        with:
          version: v0.16.0
          disable_annotations: "false"
      - name: Cache Cargo registry
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry/cache
            ~/.cargo/registry/index
            ~/.cargo/registry/src
            ~/.cargo/git/db
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - name: Cache Cargo target
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: target
          key: cargo-target-${{ runner.os }}-${{ github.job }}-${{ matrix.target }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: cargo-target-${{ runner.os }}-${{ github.job }}-${{ matrix.target }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-
      - name: Build CLI
        env:
          TABLEROCK_VERSION_OVERRIDE: ${{ needs.validate.outputs.version }}
          TARGET: ${{ matrix.target }}
          BUILD_TARGET: ${{ matrix.build_target }}
          BUILDER: ${{ matrix.builder }}
        run: |
          set -euo pipefail
          rustup target add "$TARGET"
          if test "$BUILDER" = cargo; then
            cargo build -p tablerock-cli --release --locked --target "$TARGET"
          else
            cargo zigbuild -p tablerock-cli --release --locked --target "$BUILD_TARGET"
          fi
      - name: Package CLI
        env:
          TARGET: ${{ matrix.target }}
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          set -euo pipefail
          archive="tablerock-${VERSION}-${TARGET}.tar.gz"
          tar -czf "$archive" -C "target/${TARGET}/release" tablerock
          sha256sum "$archive" | awk '{print $1}' > "${archive}.sha256"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tablerock-${{ matrix.target }}
          path: tablerock-*
          if-no-files-found: error

  build-app:
    needs: validate
    runs-on: macos-26
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      DEVELOPER_DIR: /Applications/Xcode_26.6.app/Contents/Developer
      CARGO_INCREMENTAL: "0"
      RUSTC_WRAPPER: sccache
      SCCACHE_GHA_ENABLED: "false"
      SCCACHE_CACHE_SIZE: 20G
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          install_args: rust
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba # v0.0.11
        with:
          version: v0.16.0
          disable_annotations: "false"
      - name: Cache Cargo registry
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry/cache
            ~/.cargo/registry/index
            ~/.cargo/registry/src
            ~/.cargo/git/db
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - name: Cache Cargo target
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: target
          key: cargo-target-${{ runner.os }}-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: cargo-target-${{ runner.os }}-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-
      - name: Build and package application
        env:
          TABLEROCK_APP_VERSION: ${{ needs.validate.outputs.version }}
          TABLEROCK_BUNDLE_VERSION: ${{ github.run_number }}
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          set -euo pipefail
          ./scripts/build-native-app.sh
          archive="tablerock-app-${VERSION}-aarch64-apple-darwin.zip"
          ditto -c -k --keepParent native/dist/TableRock.app "$archive"
          shasum -a 256 "$archive" | awk '{print $1}' > "${archive}.sha256"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tablerock-app-aarch64-apple-darwin
          path: tablerock-app-*
          if-no-files-found: error

  package-record:
    needs: [validate, build-cli, build-app]
    runs-on: ${{ github.event_name == 'push' && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 15
    outputs:
      version: ${{ needs.validate.outputs.version }}
      source-ref: ${{ needs.validate.outputs.source-ref }}
    permissions:
      contents: read
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts
          merge-multiple: true
      - name: Build closed release record
        env:
          VERSION: ${{ needs.validate.outputs.version }}
          SOURCE_REF: ${{ needs.validate.outputs.source-ref }}
          SOURCE_COMMIT: ${{ github.sha }}
        run: |
          set -euo pipefail
          mapfile -t subjects < <(find artifacts -maxdepth 1 -type f \( -name '*.tar.gz' -o -name '*.zip' \) | sort)
          test "${#subjects[@]}" -eq 5
          : > SHA256SUMS
          : > assets.jsonl
          mkdir signer-input
          for subject in "${subjects[@]}"; do
            name=$(basename "$subject")
            digest=$(sha256sum "$subject" | awk '{print $1}')
            test "$(cat "${subject}.sha256")" = "$digest"
            printf '%s  %s\n' "$digest" "$name" >> SHA256SUMS
            jq -cn --arg name "$name" --arg sha256 "$digest" '{name:$name,sha256:$sha256}' >> assets.jsonl
            cp "$subject" signer-input/
          done
          jq -S -n --arg source_repository "$GITHUB_REPOSITORY" \
            --arg source_ref "$SOURCE_REF" --arg source_commit "$SOURCE_COMMIT" \
            --arg version "$VERSION" --slurpfile assets assets.jsonl \
            '{schema:"velnor.package-release.v1",source_repository:$source_repository,source_ref:$source_ref,source_commit:$source_commit,version:$version,assets:$assets}' \
            > release-manifest.json
          # SHA256SUMS names the immutable signer corpus by basename. Verify
          # from that corpus directory instead of the workflow workspace.
          (cd signer-input && sha256sum --check --strict ../SHA256SUMS)
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: package-subjects
          path: signer-input
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: package-release-metadata
          path: |
            release-manifest.json
            SHA256SUMS
          if-no-files-found: error

  sign-cli:
    needs: package-record
    permissions: {attestations: write, contents: read, id-token: write}
    strategy:
      matrix:
        target: [aarch64-apple-darwin, x86_64-apple-darwin, aarch64-unknown-linux-gnu, x86_64-unknown-linux-gnu]
    uses: tailrocks/velnor-actions/.github/workflows/package-signer.yml@1e062d5bbe329873047ee8a8e79bba0811e53b65 # 2026.8.8
    with:
      artifact-name: package-subjects
      subject-path: tablerock-${{ needs.package-record.outputs.version }}-${{ matrix.target }}.tar.gz
      source-ref: ${{ needs.package-record.outputs.source-ref }}

  sign-app:
    needs: package-record
    permissions: {attestations: write, contents: read, id-token: write}
    uses: tailrocks/velnor-actions/.github/workflows/package-signer.yml@1e062d5bbe329873047ee8a8e79bba0811e53b65 # 2026.8.8
    with:
      artifact-name: package-subjects
      subject-path: tablerock-app-${{ needs.package-record.outputs.version }}-aarch64-apple-darwin.zip
      source-ref: ${{ needs.package-record.outputs.source-ref }}

  publish:
    needs: [package-record, sign-cli, sign-app]
    runs-on: ${{ github.event_name == 'push' && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts
          merge-multiple: true
      - name: Publish immutable release without replacement
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          mapfile -t files < <(find artifacts -maxdepth 1 -type f | sort)
          test "${#files[@]}" -eq 12
          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "release already exists" >&2
            exit 1
          fi
          gh release create "$TAG" --repo "$GITHUB_REPOSITORY" --verify-tag \
            --title "$TAG" --notes "Immutable signed package release $TAG." "${files[@]}"
